# Database — uses environment variables on Railway/Supabase, falls back to local values
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.kjfnppezhkojlrdemjot
spring.datasource.password=youtubelover1836

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Hibernate 6 detects the dialect automatically, and removing it avoids deprecation warnings.

# Datasource Pool & SSL (Supabase Compatibility)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
# Ensure SSL is forced if supported
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# Server
server.port=${PORT:8080}

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dkeylz0na}
cloudinary.api-key=${CLOUDINARY_API_KEY:325677424994459}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:BjDa3aru2psB8eguzoOxiNJSRGI}



# Multipart Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
